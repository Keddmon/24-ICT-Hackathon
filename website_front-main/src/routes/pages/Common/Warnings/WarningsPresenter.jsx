import { MainLayout } from "../../../../Layout";

const WarningsPresenter = ({
    id
}) => {
    return (
        <div>
            <span>
            {id}입니다
            </span>
        </div>
    );
};

export default WarningsPresenter;